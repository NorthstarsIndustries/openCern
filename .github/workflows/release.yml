# DEPRECATED: This workflow is replaced by cli-release.yml (cli-v* tags)
# and launcher-release.yml / launcher-mac-release.yml (app-v* tags).
# Kept as a no-op to avoid breaking references. Will be removed in a future cleanup.
name: Release (deprecated)

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "::warning::This workflow is deprecated."
          echo "Use 'cli-v*.*.*' tags for CLI releases (cli-release.yml)"
          echo "Use 'app-v*.*.*' tags for desktop app releases (launcher-release.yml)"
          exit 0

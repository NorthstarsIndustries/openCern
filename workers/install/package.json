{
  "name": "@opencern/install-worker",
  "version": "1.0.0",
  "description": "OpenCERN CLI install redirect Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.5.0",
    "vitest": "^3.1.1",
    "wrangler": "^4.76.0"
  }
}
